{
  "homepage": "https://SandyLudosky.github.io/voting-system",
  "scripts": {
    "predeploy": "cd client && npm run build",
    "deploy": "cd client && gh-pages -d build"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.4.1",
    "chai": "^4.3.3",
    "gh-pages": "^3.1.0",
    "truffle-assertions": "^0.9.2"
  }
}
